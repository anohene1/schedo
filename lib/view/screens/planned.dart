import 'package:flutter/material.dart';

class PlannedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Planned Screen'),
    );
  }
}
